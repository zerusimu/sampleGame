<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>武器を買う</title>
  <link rel="stylesheet" href="/css/style.css"> 
</head>
<body>

  <%- include('header'); %>
  
  <% if(locals.isLoggedIn) { %>



<table border="1">
<tr>
  <th>武器の名前</th>
  <th>攻撃力</th>
  <th>値段</th>
</tr>

  <%  buki.forEach((buki1) => { %>
    <tr>
  <td><%= buki1.namae %></td>
  <td><%= buki1.kougeki %></td>
  <td><%= buki1.nedan %></td>

  
  <td>
    <form action="/buki2/<%= buki1.id %>" method="POST">
      <input type="submit" value="買う">
    </form>
  </td>
 
</tr>
<% }) %>

</table>
 




<li><a href="/game">前の画面に戻る</a></li>

    <% } else { %>
<p>ログインされてないので、ログインしてください。</p>
      <a class="btn" href="/login">ログイン</a>
      <% } %>

</div>
</body>
</html>